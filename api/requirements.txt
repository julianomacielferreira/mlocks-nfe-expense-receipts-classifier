# API web moderna e rápida
fastapi==0.115.0
uvicorn[standard]==0.30.0
# Validação de dados (FastAPI usa por baixo)
pydantic==2.8.2
pydantic-settings==2.4.0
# Banco de dados
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
# Cliente HTTP assíncrono (para chamar Ollama)
httpx==0.27.0
# Parse XML da NF-e - muito mais rápido que xml.etree
lxml==5.2.2
# Carregar.env local (útil fora do Docker)
python-dotenv==1.0.1